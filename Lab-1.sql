USE sakila;

-- Task 1: Select the columns city_id and country_id from the city table.
SELECT city_id, country_id
FROM city;

-- Task 2: Retrieve the first and last names of actors.
SELECT first_name, last_name
FROM actor;
